{
  "name": "sporteventsserver",
  "version": "1.0.0",
  "description": "REST API for sport events calendar",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "migrate": "npx sequelize-cli db:migrate",
    "migrate-undo": "npx sequelize-cli db:migrate:undo",
    "seed-all": "npx sequelize-cli db:seed:all",
    "create-seed": "npx sequelize-cli seed:generate --name <table-seeder>",
    "start": "nodemon index"
  },
  "author": "Christoph Lansche",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "jsonwebtoken": "^8.5.1",
    "mysql2": "^2.3.3",
    "sequelize": "^6.21.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.19",
    "sequelize-cli": "^6.4.1"
  }
}
